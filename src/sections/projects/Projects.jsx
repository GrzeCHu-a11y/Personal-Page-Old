import React from "react";
import styles from "./projects.module.scss";

const Projects = () => {
  return (
    <section id="projects">
      <h2>Projects</h2>
    </section>
  );
};

export default Projects;
